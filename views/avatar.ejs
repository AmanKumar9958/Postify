<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Upload DP | Postify</title>
</head>
<body>
    <div class="w-full min-h-screen bg-gray-800 text-white flex items-center justify-center flex-col gap-2">
        <form action="/upload-avatar" method="post" enctype="multipart/form-data" class="flex flex-col gap-4 w-96 p-6 bg-gray-900 rounded-md shadow-lg border border-gray-700 transition-transform">
            <h1 class="text-2xl font-bold text-center">Upload Your Avatar</h1>
            <div class="flex flex-col gap-2">
                <label for="avatar" class="text-gray-300">Choose an image:</label>
                <input type="file" id="avatar" name="avatar" accept="image/*" class="file:cursor-pointer file:rounded-md file:border-0 file:bg-blue-600 file:text-white file:px-4 file:py-2 hover:file:bg-blue-700 transition-colors">
            </div>
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md transition-colors hover:cursor-pointer">Upload</button>
            <a href="/profile" class="text-blue-400 hover:underline text-center">Back to Profile</a>
        </form>
    </div>
    <script src="/javascript/myScripts.js"></script>
</body>
</html>