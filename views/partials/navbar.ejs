<nav class="bg-gray-800 shadow-md sticky top-0 z-50 transition-all duration-500">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        
        <!-- Logo -->
        <div class="text-2xl font-bold text-blue-500 hover:scale-105 duration-300 cursor-pointer">
        <a href="/"><i class="fa-solid fa-pen-to-square"></i> Postify</a>
        </div>

        <!-- Desktop Menu -->
        <ul class="hidden md:flex space-x-6 font-medium items-center">
            <li>
                <a href="/posts"
                class="transition duration-300 <%= req.url === '/posts' ? 'text-blue-400 underline' : 'hover:text-blue-400 text-white' %>">
                All Posts
                </a>
            </li>
            <li>
                <a href="/"
                class="transition duration-300 <%= req.url === '/' ? 'text-blue-400 underline' : 'hover:text-blue-400 text-white' %>">
                <i class="fa-solid fa-pen-to-square"></i> Post
                </a>
            </li>
            <% if (user) { %>
                <li class="relative group">
                    <button class="flex items-center space-x-2 text-white focus:outline-none hover:text-blue-400 transition duration-300">
                        <span class="hover:cursor-pointer"><%= user.username %></span>
                        <i class="fa-solid fa-chevron-down text-sm mt-1"></i>
                    </button>
                    <ul class="absolute right-0 mt-2 w-40 bg-gray-800 shadow-lg rounded-lg py-2 opacity-0 group-hover:opacity-100 scale-95 group-hover:scale-100 transition duration-200 transform origin-top-right z-50">
                        <li><a href="/profile" class="block px-4 py-2 text-sm text-white hover:bg-gray-700 transition">👤 Profile</a></li>
                        <li><a href="/logout" class="block px-4 py-2 text-sm text-red-500 hover:bg-red-500 hover:text-white transition">🚪 Logout</a></li>
                    </ul>
                </li>
            <% } else { %>
                <li>
                    <a href="/login" class="text-white hover:text-blue-400 transition">🔐 Login</a>
                </li>
                <li>
                    <a href="/register" class="text-white hover:text-blue-400 transition">📝 Register</a>
                </li>
            <% } %>
        </ul>

        <!-- Hamburger Icon -->
        <button id="menu-toggle" class="md:hidden text-white focus:outline-none">
        <i class="fa-solid fa-bars text-xl"></i>
        </button>
    </div>

    <!-- Mobile Menu -->
    <ul id="mobile-menu" class="md:hidden hidden flex flex-col px-6 pb-4 space-y-3 font-medium bg-gray-800 text-white">
        <li>
        <a href="/posts"
            class="block <%= req.url === '/posts' ? 'text-blue-400 underline' : 'hover:text-blue-400' %>">
            All Posts
        </a>
        </li>
        <li>
        <a href="/"
            class="block <%= req.url === '/' ? 'text-blue-400 underline' : 'hover:text-blue-400' %>">
            <i class="fa-solid fa-pen-to-square"></i> Post
        </a>
        </li>
        <li>
        <a href="/profile" class="block hover:text-blue-400">👤 Profile</a>
        </li>
        <li>
        <a href="/logout" class="block text-red-500 hover:text-white hover:bg-red-500 px-2 py-1 rounded-md">🚪 Logout</a>
        </li>
    </ul>
    </nav>

    <!-- Toggle Script -->
    <script>
    const toggleBtn = document.getElementById('menu-toggle');
    const mobileMenu = document.getElementById('mobile-menu');

    toggleBtn.addEventListener('click', () => {
        mobileMenu.classList.toggle('hidden');
    });
</script>
